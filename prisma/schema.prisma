generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model User {
  id Int @default(autoincrement())
  username String

  @@id([id])
}

model Food {
  id Int @default(autoincrement())
  name String

  @@id([id])
}
